# Video generation plugin env template
# Copy to .env and fill as needed

# OpenAI-compatible API credentials (falls back to global config.llm)
VIDEO_API_KEY=
VIDEO_BASE_URL=
VIDEO_MODEL=

# Optional: override plugin timeout (ms)
# Priority: .env.PLUGIN_TIMEOUT_MS > config.json.timeoutMs
#PLUGIN_TIMEOUT_MS=180000
