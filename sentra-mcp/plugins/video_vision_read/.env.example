# Per-plugin configuration for video_vision_read
# 视频分析 API 配置

# API Base URL（支持 OpenAI 兼容接口）
VIDEO_VISION_BASE_URL=https://yuanplus.cloud/v1

# API Key
VIDEO_VISION_API_KEY=sk-REPLACE_ME

# 模型名称（需要支持视频分析的模型，如 gpt-4o, gpt-4-turbo-vision）
VIDEO_VISION_MODEL=qwen3-vl

# 单个视频最大大小限制（MB），默认 50MB
# 超过此大小的视频将被拒绝处理
# 注意：某些 API 可能有更严格的限制（如 OpenAI 通常限制在 25MB）
VIDEO_VISION_MAX_SIZE_MB=50

# Optional: override plugin timeout (ms)
# Priority: .env.PLUGIN_TIMEOUT_MS > config.json.timeoutMs
# 视频处理通常比图片耗时更长，建议设置较大的超时时间
PLUGIN_TIMEOUT_MS=360000
