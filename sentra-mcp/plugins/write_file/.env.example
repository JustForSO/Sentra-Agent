# 基础输出目录（默认：artifacts）
# 相对路径将基于此目录解析，绝对路径直接写入
# 示例：artifacts, output, files, data
WRITE_FILE_BASE_DIR=artifacts

# 字体文件目录（相对于插件目录，默认：fonts）
# 示例：fonts, custom-fonts, assets/fonts
WRITE_FILE_FONT_DIR=fonts

# 字体文件名（默认：NotoSansSC-Regular.otf）
# 支持的格式：.otf, .ttf, .ttc
# 示例：NotoSansSC-Regular.otf, SimSun.ttf, MyCustomFont.otf
WRITE_FILE_FONT_NAME=AaRiFuYiRiKeAiYuan-2.ttf