# music_card plugin env template (WebSocket SDK + NetEase 163)
# Copy to .env and fill as needed

# WebSocket SDK (ws_rpc) endpoint
WS_SDK_URL=ws://localhost:6702
# Optional timeout (ms)
WS_SDK_TIMEOUT_MS=15000
# Arg style for wsCall args: 'object' -> [{ user_id|group_id, message }], 'pair' -> [id, message]
WS_SDK_ARG_STYLE=pair
# Send paths to try (main + specific); adjust to your bot SDK
# NapCat SDK uses send.private / send.group
WS_SDK_SEND_PATH=
WS_SDK_SEND_PATH_PRIVATE=send.private
WS_SDK_SEND_PATH_GROUP=send.group

# NetEase 163 search defaults
MUSIC163_SEARCH_LIMIT=6
# Optional: NetEase MUSIC_U cookie (to improve play URL acquisition)
MUSIC163_COOKIE_MUSIC_U=

# Optional: override plugin timeout (ms)
# Priority: .env.PLUGIN_TIMEOUT_MS > config.json.timeoutMs
#PLUGIN_TIMEOUT_MS=40000
