# html_to_app 插件配置示例
# 将 HTML/项目描述转换为桌面应用（Electron）

# === LLM 配置（生成项目代码） ===
# Base URL 默认使用全局配置
HTML_TO_APP_BASE_URL=https://yuanplus.cloud/v1
HTML_TO_APP_API_KEY=
HTML_TO_APP_MODEL=gpt-4o

# === 应用框架配置 ===
# 支持的框架: electron-vanilla | electron-react | electron-vue
# vanilla: 原生 HTML/CSS/JS
# react: React + Vite
# vue: Vue 3 + Vite
HTML_TO_APP_DEFAULT_FRAMEWORK=electron-vanilla

# === 项目生成配置 ===
# 输出目录基准路径（相对于项目根目录）
HTML_TO_APP_OUTPUT_DIR=artifacts/apps

# === 包管理器配置 ===
# 可选值: npm | pnpm | cnpm | yarn
HTML_TO_APP_PACKAGE_MANAGER=npm

# 包管理器安装命令参数（可选）
# 例如：--registry=https://registry.npmmirror.com
HTML_TO_APP_INSTALL_ARGS=

# === Electron 下载镜像配置（重要！）===
# 解决 electron-builder 从 GitHub 下载超时问题
HTML_TO_APP_ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/
HTML_TO_APP_ELECTRON_BUILDER_BINARIES_MIRROR=https://npmmirror.com/mirrors/electron-builder-binaries/

# 代理设置（可选）
# 如果使用代理，可以设置 HTTP_PROXY 和 HTTPS_PROXY
HTML_TO_APP_HTTP_PROXY=
HTML_TO_APP_HTTPS_PROXY=

# === 自动化流程配置 ===
# 是否自动安装依赖（需要本地 Node.js 环境）
HTML_TO_APP_AUTO_INSTALL=true

# 是否自动打包为可执行文件（需要 electron-builder）
HTML_TO_APP_AUTO_BUILD=true

# 是否压缩打包结果为 zip（方便分发）
HTML_TO_APP_AUTO_ZIP=true

# 打包完成后是否删除临时构建文件（保留 zip）
HTML_TO_APP_CLEAN_BUILD=true

# === 插件超时配置 ===
# 生成项目可能耗时较长，建议设置较大超时
# 注意：.env 中的 PLUGIN_TIMEOUT_MS 会覆盖 config.json 中的 timeoutMs
# 超时配置（首次打包建议更长）
PLUGIN_TIMEOUT_MS=1200000  # 20分钟（包含下载 Electron 时间）
