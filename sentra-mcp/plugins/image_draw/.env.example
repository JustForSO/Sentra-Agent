# Per-plugin configuration for image_draw
# Default model used when args.model is not provided (must be included in config.json "models")
DRAW_MODEL=luminaArchitect-v1-plus
DRAW_BASE_URL=https://yuanplus.cloud/v1
DRAW_API_KEY=sk-REPLACE_ME
