{
  "name": "image_draw",
  "description": "根据文本提示生成或绘制图片，返回包含图片链接的 Markdown 内容。",
  "inputSchema": {
    "type": "object",
    "properties": {
      "prompt": { 
        "type": "string", 
        "description": "绘图指令/主题描述，英文描述的编辑/生成指令（必须为英文）。重要规则：如果描述中包含男性角色，必须在开头添加 '1boy'（一个男性）或 'xboy'（x个男性，如 2boy）；如果包含女性角色，必须添加 '1girl' 或 'xgirl'。例如：'1boy, a handsome man' 或 '1girl, beautiful woman' 或 '2boy 1girl, group photo'。"
      },
      "schedule": {
        "oneOf": [
          {
            "type": "string",
            "description": "当用户需要延迟处理/延迟反馈时使用：例如 '5分钟后'、'30秒后'、'明天09:00'、ISO 字符串。"
          },
          {
            "type": "object",
            "properties": {
              "when": {
                "type": "string",
                "description": "时间表达：如 '5分钟后' / '明天09:00' / ISO 字符串"
              },
              "language": {
                "type": "string",
                "description": "语言标识，例如 zh|en（默认 zh）"
              },
              "timezone": {
                "type": "string",
                "description": "IANA 时区名称，例如 Asia/Shanghai"
              }
            },
            "additionalProperties": true
          }
        ],
        "description": "可选：当用户需要延迟处理/延迟反馈时使用；仅控制反馈时间，不延迟执行。若到点未完成，将先反馈进行中状态。"
      }
    },
    "required": ["prompt"],
    "additionalProperties": true
  },
  "timeoutMs": 120000,
  "meta": {
    "realWorldAction": "图像生成/绘制",
    "responseStyle": "纯文本（Markdown）",
    "responseExample": "Here is a drawing of a cat:\n\n![image](E:/sentra-agent/sentra-mcp/artifacts/draw_1696231234567_0.png)"
  }
}
