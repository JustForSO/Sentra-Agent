{
  "name": "web_render_image",
  "description": "将 HTML 代码/URL/本地文件渲染为图片，支持完整页面或指定元素截图。可注入自定义样式/脚本，适用于生成模板图片、预览页面、截取组件等场景。",
  "inputSchema": {
    "type": "object",
    "properties": {
      "html": {
        "type": "string",
        "description": "HTML 源码（可以是完整页面或片段）"
      },
      "url": {
        "type": "string",
        "description": "网页地址（http/https）"
      },
      "file": {
        "type": "string",
        "description": "本地 HTML 文件路径"
      },
      "selector": {
        "type": "string",
        "description": "可选：CSS 选择器，仅截取匹配的元素"
      },
      "fullPage": {
        "type": "boolean",
        "description": "是否截取整个页面（默认 true，有 selector 时忽略）"
      },
      "css": {
        "type": "string",
        "description": "可选：注入的自定义 CSS 样式代码"
      },
      "js": {
        "type": "string",
        "description": "可选：注入的自定义 JavaScript 代码"
      },
      "wait_for": {
        "type": "string",
        "description": "可选：等待策略 - auto(智能等待) | load(页面加载完成) | networkidle(网络空闲)"
      },
      "schedule": {
        "oneOf": [
          {
            "type": "string",
            "description": "当用户需要延迟处理/延迟反馈时使用：例如 '5分钟后'、'30秒后'、'明天09:00'、ISO 字符串。"
          },
          {
            "type": "object",
            "properties": {
              "when": {
                "type": "string",
                "description": "时间表达：如 '5分钟后' / '明天09:00' / ISO 字符串"
              },
              "language": {
                "type": "string",
                "description": "语言标识，例如 zh|en（默认 zh）"
              },
              "timezone": {
                "type": "string",
                "description": "IANA 时区名称，例如 Asia/Shanghai"
              }
            },
            "additionalProperties": true
          }
        ],
        "description": "可选：当用户需要延迟处理/延迟反馈时使用；仅控制反馈时间，不延迟执行。若到点未完成，将先反馈进行中状态。"
      }
    },
    "required": [],
    "additionalProperties": true
  },
  "timeoutMs": 60000,
  "meta": {
    "realWorldAction": "渲染网页为图片",
    "responseStyle": "纯文本（Markdown 图片链接）",
    "responseExample": "![render](E:/sentra-agent/sentra-mcp/artifacts/render_1234567890.png)"
  }
}
