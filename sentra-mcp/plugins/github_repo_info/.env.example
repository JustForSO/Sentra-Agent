# GitHub 仓库查询插件环境变量模板
# 复制为 .env 并按需填写

# 可选：GitHub 访问令牌（强烈建议配置以提升速率限制与权限）
# 生成方式：https://github.com/settings/tokens
GITHUB_TOKEN=
# 亦可使用 GH_TOKEN（等同优先级稍低于 GITHUB_TOKEN）
GH_TOKEN=

# 可选：自定义 GitHub API 基地址（企业版或代理场景）
# 默认：https://api.github.com
GITHUB_API_BASE=https://api.github.com

# 可选：GitHub API 版本
# 参考：https://docs.github.com/en/rest/overview/api-versions
GITHUB_API_VERSION=2022-11-28

# 可选：覆盖本插件的超时时长（毫秒）
# 优先级：.env.PLUGIN_TIMEOUT_MS > config.json.timeoutMs
# 默认见 plugins/github_repo_info/config.json
#PLUGIN_TIMEOUT_MS=90000

# 可选：默认返回数量上限（可被入参 max_* 覆盖）
GITHUB_MAX_COMMITS=5
GITHUB_MAX_CONTRIBUTORS=5
GITHUB_MAX_TAGS=10
GITHUB_README_PREVIEW_CHARS=400

# 可选：是否默认包含更多信息（可被入参 include_* 覆盖）
GITHUB_INCLUDE_LANGUAGES=false
GITHUB_INCLUDE_TOPICS=false
GITHUB_INCLUDE_RELEASES=false
GITHUB_INCLUDE_TAGS=false
GITHUB_INCLUDE_README_PREVIEW=false
GITHUB_INCLUDE_COMMUNITY_PROFILE=false
GITHUB_INCLUDE_STATS_COMMIT_ACTIVITY=false
