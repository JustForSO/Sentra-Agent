# 天气插件配置 - Weather Plugin Configuration
# 复制此文件为 .env 并填写你的 API 密钥
# Copy this file to .env and fill in your API key

# 和风天气 API 配置 - QWeather API Configuration
# 必填：API 密钥，在 https://dev.qweather.com/ 申请
# Required: API Key, get it from https://dev.qweather.com/
WEATHER_API_KEY=

# 可选：API 主机地址（默认：devapi.qweather.com）
# Optional: API host (default: devapi.qweather.com)
# 免费版使用：devapi.qweather.com
# 付费版使用：api.qweather.com
WEATHER_API_HOST=devapi.qweather.com

# 可选：默认语言（默认：zh）
# Optional: Default language (default: zh)
# 支持：zh(中文), en(英文), jp(日文), kr(韩文) 等
WEATHER_LANG=zh

# 可选：默认单位制（默认：m）
# Optional: Default unit system (default: m)
# m=公制（摄氏度、公里）, i=英制（华氏度、英里）
WEATHER_UNIT=m
