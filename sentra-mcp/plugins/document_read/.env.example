# Per-plugin configuration for document_read
# 文档和代码文件解析插件配置

# Optional: override plugin timeout (ms)
# Priority: .env.PLUGIN_TIMEOUT_MS > config.json.timeoutMs
# 默认 60000 (60秒)，大文档可增加此值
#PLUGIN_TIMEOUT_MS=60000

# Maximum file size limit in MB
# 默认 10MB，可根据需要调整
# 建议值：文档 10-50MB，代码 1-10MB，日志 50-100MB
DOC_MAX_FILE_SIZE_MB=10

# Optional: Default encoding for text/code files
# 留空则自动检测，支持: utf8, gbk, gb2312, shift_jis, big5 等
# 现代代码文件通常使用 utf8
#DOC_DEFAULT_ENCODING=utf8
