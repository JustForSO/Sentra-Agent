# ============================================
# Sentra Agent 主配置文件
# ============================================
# 注意：各子模块有独立的配置文件
# - sentra-rag/.env       (RAG配置)
# - sentra-mcp/.env       (MCP配置)
# - sentra-prompts/.env   (Prompts配置)
# - sentra-emo/.env       (情绪分析配置)
# ============================================

# -------------------- 基础 AI 配置 --------------------
# OpenAI 兼容 API 配置（Agent 基础配置，可被子模块继承）
API_BASE_URL=https://api.openai.com/v1
API_KEY=your_api_key_here
MODEL_NAME=gpt-4o-mini
TEMPERATURE=0.7
MAX_TOKENS=2000
MAX_RETRIES=3
TIMEOUT=60000

# -------------------- 功能开关 --------------------
# 控制 Agent 集成哪些功能模块
ENABLE_SEGMENTATION=true        # 分词功能（本地，无需额外配置）
ENABLE_RAG=true                 # RAG检索（需要配置 sentra-rag/.env）
ENABLE_PROMPTS=true             # 动态提示词（需要配置 sentra-prompts/.env）
ENABLE_MCP=true                 # MCP工具调用（需要配置 sentra-mcp/.env）
ENABLE_MEMORY_SAVE=true         # 记忆保存到RAG

# -------------------- RAG 集成配置 --------------------
# Agent 层面的 RAG 调用参数
RAG_KEYWORD_LIMIT=5             # 关键词检索数量限制
RAG_VECTOR_LIMIT=3              # 向量检索数量限制
RAG_THRESHOLD=0.7               # 相似度阈值

# ⚠️ RAG 的详细配置（Neo4j、Embedding等）请在 sentra-rag/.env 中配置

# -------------------- 其他配置 --------------------
# 日志级别
LOG_LEVEL=info

# 会话默认配置
DEFAULT_CONVERSATION_ID=conv_default
DEFAULT_USER_ID=user_default
